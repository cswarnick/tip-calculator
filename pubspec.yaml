name: bizcardapp
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.1.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  flutter_bloc: ^8.1.3 # This package allows you to create Cubit/state classes, and has exports for things like BlocBuilder and BlocProvider
  equatable: ^2.0.5 # This package allows you to compare objects for equality, and allows your classes to extend equatable.

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
